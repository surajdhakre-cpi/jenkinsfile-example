pipeline {
 
  agent any
  
  stages {
    stage("build"){
      steps {
       echo "I am building...." 
      }
    }
    stage("test"){
      steps {
       echo "Testing the app...." 
      }
    }
    stage("deploy"){
      steps {
       echo "Deploying the app...." 
      }
    }
  }
}
